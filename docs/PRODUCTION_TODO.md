# TODO: путь в продакшн

Текущее состояние (2026-09-19): рабочий MVP-скелет — Room, Home/Card/About,
ядро пересчёта с baseline/displayed, тёмная тема. Ниже — что остаётся до
публикации в Play Store, сгруппировано по приоритету.

## 1. Функциональность из ТЗ, которой ещё нет в коде

- [x] **Автодополнение ингредиентов** (ТЗ 3.3) — `IngredientNameDao.suggest()` уже
      есть, не хватает UI: dropdown/`ExposedDropdownMenu` на поле "Ingredient" в
      `RecipeCardScreen.kt`.
- [ ] **Штучные ингредиенты** (ТЗ 3.5, добавлено после MVP-скелета) — переключатель
      "целыми единицами" на строке, округлённое отображение с точным значением
      рядом, запоминание выбора по имени ингредиента в автодополнении.
- [ ] **Лимит 3 бесплатных карточки + разовая покупка** (ТЗ 5) — Billing Library
      ещё не подключена; нужен `RecipeDao.countRecipes()` (уже есть) + paywall-диалог
      + проверка `queryPurchasesAsync` при старте.
- [ ] Фото карточки и заметки (ТЗ 3.4, опционально) — можно отложить на пост-MVP.

## 2. Плейсхолдеры, которые нельзя публиковать как есть

- [ ] `AboutScreen.kt`: `PRIVACY_POLICY_URL` и `CONTACT_EMAIL` — сейчас
      `example.com` / `support@example.com`. Нужны реальный контакт и
      захостенная страница политики конфиденциальности (см. п. 4).
- [ ] Иконка приложения (`ic_launcher_foreground.xml`) — сейчас две точки
      (":", символ пропорции) как временная заглушка. Нужна финальная адаптивная
      иконка (мин. foreground/background, желательно + monochrome-слой для
      Android 13+ themed icons).
- [ ] `app_name` в `strings.xml` = "Proportions" — подтвердить финальное
      название перед публикацией (в ТЗ оно помечено как рабочее).

## 3. Инженерия релиза

- [ ] Настроить `signingConfigs.release` в `app/build.gradle.kts` по образцу
      `Trainer/docs/RELEASE_SIGNING.md` (keystore вне репозитория, путь через
      `local.properties` → `keystore.properties`, чтобы `assembleDebug` не
      требовал секретов на чужой машине).
- [ ] Сгенерировать и надёжно сохранить (KeePass/менеджер паролей +
      бэкап‑копия) release keystore; включить **Play App Signing** при первом
      создании записи приложения в Play Console.
- [ ] Включить `isMinifyEnabled = true` + R8/ProGuard для release-сборки,
      прогнать `assembleRelease` и убедиться, что Room/Compose/Navigation не
      ломаются обфускацией (при необходимости — добавить keep-правила в
      `proguard-rules.pro`).
- [ ] Проверить `versionCode`/`versionName` перед каждой загрузкой в Play
      Console (сейчас `1` / `"1.0"`).

## 4. Юридическое / комплаенс

- [ ] Написать и захостить страницу политики конфиденциальности (обязательна
      для Play Console из-за локального хранения данных и, позже, Billing).
      Подойдёт статическая страница (GitHub Pages и т.п.) — ссылка нужна и в
      Play Console, и в `AboutScreen`.
- [ ] Заполнить **Data safety** форму в Play Console (какие данные собираются:
      по факту — никаких, всё локально + возможные покупки через Billing).
- [ ] Пройти **Content rating** опросник.

## 5. Тестирование

- [ ] Unit-тесты на `RecipeCardViewModel`/пересчёт: baseline = 0, один
      ингредиент, повторный пересчёт без пересохранения не должен дрейфовать
      от исходного baseline (граничные случаи описаны в `docs/PLAN.md` п.8).
- [ ] Unit-тесты DAO (in-memory Room): `IngredientNameDao` — сортировка по
      частоте, upsert счётчика; `RecipeDao.saveRecipe` — замена строк не теряет
      и не дублирует данные.
- [ ] Ручной прогон на реальном устройстве: создание/редактирование/удаление
      карточки, лимит 3 карточек + paywall, автодополнение, светлая и тёмная
      тема, Android 8.0 (minSdk 26) если есть доступ к старому устройству/образу.
- [ ] `assembleRelease` + установка релизной сборки на устройство (а не только
      debug) — R8 иногда ломает то, что работало в debug.

## 6. Публикация

- [ ] Иконка, скриншоты (минимум телефон, желательно планшет), feature graphic,
      короткое и полное описание под ASO-запросы из ТЗ п.7 ("ratio calculator
      recipe", "ingredient proportion calculator").
- [ ] Создать запись приложения в Play Console, залить `.aab`
      (`./gradlew bundleRelease`).
- [ ] Закрытое тестирование → открытое тестирование → продакшн (поэтапный
      прогон, а не сразу на 100% пользователей).

## 7. После первого релиза

- [ ] Настроить минимальный мониторинг падений (Trainer уже использует Firebase
      Crashlytics — можно переиспользовать тот же Firebase-проект или завести
      новый под `az.dahcraf.proportions`).
- [ ] Отслеживать отзывы/рейтинг первые недели, ASO-метрики по низкочастотным
      запросам (ожидания по деньгам — см. ТЗ п.7, это не про "быстрый взрыв").
